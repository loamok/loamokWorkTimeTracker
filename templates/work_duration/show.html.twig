{% extends 'base.html.twig' %}

{% block menu_links %}
    {% include "quick_cfg/_menu.html.twig" %}
{% endblock %}

{% block title %}WorkDuration{% endblock %}

{% block body %}
    <h1>WorkDuration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_duration.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work_duration.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ work_duration.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_duration_index') }}">back to list</a>

    <a href="{{ path('work_duration_edit', {'id': work_duration.id}) }}">edit</a>

    {{ include('work_duration/_delete_form.html.twig') }}
{% endblock %}
