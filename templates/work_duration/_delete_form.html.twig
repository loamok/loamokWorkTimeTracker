<form method="post" action="{{ path('work_duration_delete', {'id': work_duration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work_duration.id) }}">
    <button class="btn">Delete</button>
</form>
