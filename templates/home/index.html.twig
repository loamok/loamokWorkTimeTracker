{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <turbo-frame id="nav_block">
        <h1>Hello {{ controller_name }} : Index ! ✅</h1>
        This friendly message is coming from: HomeController:index action<br />
        Go to
        {% if app.user %}
            <a href="{{ path('app_plop') }}">Plop</a><br />
        {%  else %}    
            <a href="{{ path('app_login') }}" data-turbo-frame="_top">Login</a><br />
        {%  endif %}    
    </turbo-frame>
</div>
{% endblock %}
