{% extends 'base.html.twig' %}

{% block menu_links %}
    {% include "quick_cfg/_menu.html.twig" %}
{% endblock %}

{% block title %}WorkingDurationStore{% endblock %}

{% block body %}
    <h1>WorkingDurationStore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ working_duration_store.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ working_duration_store.start ? working_duration_store.start|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ working_duration_store.end ? working_duration_store.end|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('working_duration_store_index') }}">back to list</a>

    <a href="{{ path('working_duration_store_edit', {'id': working_duration_store.id}) }}">edit</a>

    {{ include('working_duration_store/_delete_form.html.twig') }}
{% endblock %}
